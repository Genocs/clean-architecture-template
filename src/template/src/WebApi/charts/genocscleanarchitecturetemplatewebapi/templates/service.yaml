apiVersion: v1
kind: Service
metadata:
  name: {{ template "genocscleanarchitecturetemplatewebapi.fullname" . }}
  labels:
    app: {{ template "genocscleanarchitecturetemplatewebapi.name" . }}
    chart: {{ template "genocscleanarchitecturetemplatewebapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "genocscleanarchitecturetemplatewebapi.name" . }}
    release: {{ .Release.Name }}
